<!DOCTYPE html>
<html>
<style>
body {background-color: powderblue;}
table, th, td {
  border:1px solid black;
}
</style>
<body>
<h1><%= @category.name %></h1>

<%= link_to "create restaurants",  new_category_restaurant_path(@category)%>

<style>
body {background-color: powderblue;}
table, th, td {
  border:1px solid black;
}
</style>
<body>
<table style="width:100%">
  <tr>
    <th>Name</th>
    <th>Address</th>
    <th>Actions</th>
  </tr>
<tbody>   
      <% @restaurants.each do |restaurant| %>   
          <tr>   
            <td><%= restaurant.name %></td>   
            <td><%= restaurant.address %></td>
               <div>   
               <td>   
                <%= link_to 'Show',  category_restaurant_path(id: restaurant.id,category_id:@category.id) %>   
                <%= link_to 'Edit', edit_category_restaurant_path(id: restaurant.id,category_id:@category.id) %>   
                <%= link_to 'Delete', category_restaurant_path(id: restaurant.id,category_id:@category.id), 
                       method: :delete,
                       data: { confirm: "Are you sure?" } %>   
              </div>   
            </td>   
          </tr>   
      <% end %>   
      </tbody>   
    </table>   
  </body>   
</html>  



